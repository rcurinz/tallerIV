const app = new Vue({
    el: '#app',
    data:{
        //Aqui van las variables (ej: contador=0, iniciar=False, nombres = [], etc)

    },
    methods:{
        //Aqui van los metodos o funciones (se escriben nombre_metodo(){}, )
        
    },
    computed:{

    }

})